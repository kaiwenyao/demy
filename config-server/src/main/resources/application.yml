server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///Users/kaiwenyao/Documents/demy-config-repo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
